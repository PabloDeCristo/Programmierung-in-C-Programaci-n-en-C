#include <stdio.h>

int main(){
    float a=0, b=0, c=0;
    printf("Ingrese los valores de a, b y c: "); scanf("%f %f %f", &a, &b, &c);
    printf("La media aritmetica o promedio es = %.f\n", (a+b+c)/3);
return 0;
}